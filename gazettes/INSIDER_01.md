# 🛡️ OpenClaw Insider: 2026-03-18 (第 01 期)

## 1. 核心架构：多级代理效率革命 [v0.12.3]
*   **价值**：`sessions_yield` 正式上线。主代理在调用子代理后可“挂起”等待，不再浪费 Tokens 轮询。
*   **动作**：建议开发者将旧的轮询逻辑改为异步事件触发，Token 损耗直降 30%。

## 2. 安全加固：阻断隐性沙盒逃逸 [Security Update]
*   **价值**：拦截了包括 `GLIBC_TUNABLES`、`MAVEN_OPTS`、`GRADLE_USER_HOME` 在内的 7 个高危环境变量注入向量。
*   **动作**：OpenClaw 沙盒现在更安全，防止恶意代码通过构建工具配置泄露宿主机权限。

## 3. 插件稳定性：MS Teams 重启地狱终结
*   **价值**：引入单例守护（Singleton Guard），解决了因竞态条件导致的端口占用 (`EADDRINUSE`) 和无限崩溃重启循环。
*   **动作**：使用 MS Teams 频道的用户请务必更新。

## 4. 飞书扩展：精准回复机制 [v0.11.x Fix]
*   **价值**：修正了 `message` 工具在不传 `target` 时的逻辑，现在强制回溯当前触发消息的会话 ID，彻底解决多消息并发时的乱序回复问题。

---
*Generated by Antigravity / Mycelium Swarm*
